<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Orderly.App;component/Views/Resources/MainWindowResources.xaml" />
        <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainWindowInlineStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Node 1: 待确认 -->
    <Style x:Key="StepperNode1BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="paid_pending_confirm">
                <Setter Property="Background" Value="{DynamicResource BlueSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BlueTextBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource BlueTextBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode1LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="paid_pending_confirm">
                <Setter Property="Foreground" Value="{DynamicResource BlueTextBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="pending_make"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Line 1-2 -->
    <Style x:Key="StepperConnector1Style" TargetType="Rectangle">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="pending_make"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Node 2: 待制作 -->
    <Style x:Key="StepperNode2BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#D0D4D9" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="pending_make">
                <Setter Property="Background" Value="{DynamicResource WarmSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource WarmTextBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource WarmTextBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode2LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="pending_make">
                <Setter Property="Foreground" Value="{DynamicResource WarmTextBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Line 2-3 -->
    <Style x:Key="StepperConnector2Style" TargetType="Rectangle">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Node 3: 制作中 -->
    <Style x:Key="StepperNode3BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#D0D4D9" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making">
                <Setter Property="Background" Value="{DynamicResource PurpleSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource PurpleTextBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource PurpleTextBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode3LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="making">
                <Setter Property="Foreground" Value="{DynamicResource PurpleTextBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Line 3-4 -->
    <Style x:Key="StepperConnector3Style" TargetType="Rectangle">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Node 4: 待发货 -->
    <Style x:Key="StepperNode4BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#D0D4D9" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship">
                <Setter Property="Background" Value="{DynamicResource BlueSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BlueTextBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource BlueTextBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode4LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="ready_to_ship">
                <Setter Property="Foreground" Value="{DynamicResource BlueTextBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Line 4-5 -->
    <Style x:Key="StepperConnector4Style" TargetType="Rectangle">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Node 5: 已发货 -->
    <Style x:Key="StepperNode5BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#D0D4D9" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped">
                <Setter Property="Background" Value="{DynamicResource PrimaryLightBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource PrimaryBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Background" Value="{DynamicResource SurfaceSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource HeadingBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource HeadingBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode5LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="shipped">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Foreground" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Line 5-6 -->
    <Style x:Key="StepperConnector5Style" TargetType="Rectangle">
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed"><Setter Property="Fill" Value="{DynamicResource HeadingBrush}" /></DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Node 6: 已完成 -->
    <Style x:Key="StepperNode6BorderStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#D0D4D9" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource AccentTextBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource AccentTextBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StepperNode6LabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding SelectedStringNarrationOrderDetail.FulfillmentStatus}" Value="completed">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
