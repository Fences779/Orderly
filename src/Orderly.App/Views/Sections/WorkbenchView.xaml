<UserControl x:Class="Orderly.App.Views.Sections.WorkbenchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Sections="clr-namespace:Orderly.App.Views.Sections">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/MainWindowResources.xaml" />
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainWindowInlineStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid Margin="0,0,0,12" Background="Transparent">
            <Grid.RowDefinitions>
                <!-- 0. 独立头部区域 -->
                <RowDefinition Height="Auto" />
                <!-- 1. 五个概览卡片 -->
                <RowDefinition Height="Auto" />
                <!-- 2. 中间趋势与履约压力 -->
                <RowDefinition Height="*" MinHeight="340" />
                <!-- 3. 底部库存与现金流 -->
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Sections:WorkbenchHeader Grid.Row="0" />

            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Sections:WorkbenchOverviewCardsGroupA Grid.ColumnSpan="9" />
                <Sections:WorkbenchOverviewCardsGroupB Grid.ColumnSpan="9" />
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.7*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Sections:WorkbenchTrendCard Grid.Column="0" />
                <Sections:WorkbenchFulfillmentPressure Grid.Column="2" />
            </Grid>

            <Sections:WorkbenchBottomCards Grid.Row="3" />
        </Grid>
    </Grid>
</UserControl>
