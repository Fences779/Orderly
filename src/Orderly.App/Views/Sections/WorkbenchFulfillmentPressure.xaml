<UserControl x:Class="Orderly.App.Views.Sections.WorkbenchFulfillmentPressure"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/MainWindowResources.xaml" />
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainWindowInlineStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="20,18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.03"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock Text="今日履约压力" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" />
            </StackPanel>

            <!-- 履约压力卡片 -->
            <StackPanel Grid.Row="1" Margin="0,18,0,0" VerticalAlignment="Top">
                <!-- 待制作 -->
                <Border Background="#FBFAF6" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="0,0,0,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18" Background="{DynamicResource WarmSoftBrush}" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource WarmTextBrush}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <Grid>
                                <TextBlock Text="待制作" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource HeadingBrush}" HorizontalAlignment="Left" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="占未完成订单 " FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    <TextBlock Text="60%" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" />
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="18 单" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" Margin="0,4,0,4" />
                            <ProgressBar Value="{Binding StringNarrationWorkbenchPendingMakeCount, Mode=OneWay}" Maximum="{Binding StringNarrationWorkbenchUnfinishedOrderCount, Mode=OneWay}" Height="6" Background="#E5E7EC" Foreground="{DynamicResource WarmTextBrush}" BorderThickness="0" Margin="0,4,0,0">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border CornerRadius="3" Background="{TemplateBinding Background}"/>
                                            <Border x:Name="PART_Indicator" CornerRadius="3" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 待发货 -->
                <Border Background="#FBFAF6" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18" Background="{DynamicResource PurpleSoftBrush}" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource PurpleTextBrush}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1">
                            <Grid>
                                <TextBlock Text="待发货" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource HeadingBrush}" HorizontalAlignment="Left" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="占未完成订单 " FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    <TextBlock Text="30%" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" />
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="9 单" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" Margin="0,4,0,4" />
                            <ProgressBar Value="{Binding StringNarrationWorkbenchReadyToShipCount, Mode=OneWay}" Maximum="{Binding StringNarrationWorkbenchUnfinishedOrderCount, Mode=OneWay}" Height="6" Background="#E5E7EC" Foreground="{DynamicResource PurpleTextBrush}" BorderThickness="0" Margin="0,4,0,0">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Grid>
                                            <Border CornerRadius="3" Background="{TemplateBinding Background}"/>
                                            <Border x:Name="PART_Indicator" CornerRadius="3" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- 中间弹性占位 -->
            <Border Grid.Row="2" />

            <!-- 底部：当日实际总单数 -->
            <Border Grid.Row="3" BorderBrush="#F1F2F5" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="当日实际总单数：" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                    <TextBlock Text="{Binding StringNarrationWorkbenchTodayOrderCount}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource HeadingBrush}" />
                    <TextBlock Text=" 单" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
                </StackPanel>
            </Border>
            <Border Grid.Row="3" />
        </Grid>
    </Border>
</UserControl>
