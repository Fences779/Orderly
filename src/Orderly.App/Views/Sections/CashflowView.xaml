<UserControl x:Class="Orderly.App.Views.Sections.CashflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Sections="clr-namespace:Orderly.App.Views.Sections">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainSharedResources.xaml" />
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainComponentStyles.A.xaml" />
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/Main/MainComponentStyles.C.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="24,12,24,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Sections:CashflowHeader Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3.1*" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="CashflowFirstRow" Height="Auto" />
                    <RowDefinition x:Name="CashflowTrendRow" Height="*" />
                    <RowDefinition x:Name="CashflowBreakdownRow" Height="Auto" />
                </Grid.RowDefinitions>

                <Sections:CashflowKpiCards Grid.Row="0" />
                <Sections:CashflowTrendChartCard x:Name="CashflowTrendCard" Grid.Row="1" />
                <Sections:CashflowBreakdownCards Grid.Row="2" />
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Sections:CashflowAdviceCard Grid.Row="0" />
                <Sections:CashflowUpcomingCashCard Grid.Row="2" />
            </Grid>
        </Grid>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,24,0,0" FontSize="11" Foreground="{StaticResource CaptionTextBrush}">
            <Run Text="ℹ️ 数据统计截止至 " />
            <Run Text="{Binding CashflowUpdatedAtText, Mode=OneWay, FallbackValue='2024-06-12 23:59:59'}" />
            <Run Text="，部分数据可能存在延迟，仅供参考。" />
        </TextBlock>
    </Grid>
</UserControl>
