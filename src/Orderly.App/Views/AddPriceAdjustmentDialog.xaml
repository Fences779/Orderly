<Window x:Class="Orderly.App.Views.AddPriceAdjustmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增改价"
        Width="440"
        Height="350"
        MinWidth="400"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        AutomationProperties.AutomationId="AddPriceAdjustmentDialog">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="原价" FontWeight="SemiBold" />
        <TextBox x:Name="OriginalAmountTextBox" Grid.Row="1" Margin="0,8,0,14" />

        <TextBlock Grid.Row="2" Text="调整后价格" FontWeight="SemiBold" />
        <TextBox x:Name="AdjustedAmountTextBox" Grid.Row="3" Margin="0,8,0,14" />

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="改价原因" FontWeight="SemiBold" />
            <TextBox x:Name="ReasonTextBox"
                     Grid.Row="1"
                     Margin="0,8,0,0"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Width="78" Margin="0,0,8,0" IsCancel="True"
                    AutomationProperties.AutomationId="AddPriceAdjustmentDialog_Cancel" />
            <Button Content="确认" Width="78" IsDefault="True" Click="ConfirmButton_Click"
                    AutomationProperties.AutomationId="AddPriceAdjustmentDialog_Confirm" />
        </StackPanel>
    </Grid>
</Window>
