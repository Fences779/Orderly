<Window x:Class="Orderly.App.Views.AddNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增备注"
        Width="420"
        Height="300"
        MinWidth="380"
        MinHeight="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        AutomationProperties.AutomationId="AddNoteDialog">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="备注类型" FontWeight="SemiBold" />
        <ComboBox x:Name="TypeComboBox" Grid.Row="1" Margin="0,8,0,14" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="备注内容" FontWeight="SemiBold" />
            <TextBox x:Name="ContentTextBox"
                     Grid.Row="1"
                     Margin="0,8,0,0"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" Width="78" Margin="0,0,8,0" IsCancel="True"
                    AutomationProperties.AutomationId="AddNoteDialog_Cancel" />
            <Button Content="确认" Width="78" IsDefault="True" Click="ConfirmButton_Click"
                    AutomationProperties.AutomationId="AddNoteDialog_Confirm" />
        </StackPanel>
    </Grid>
</Window>
