<Window x:Class="Orderly.App.Views.PinUnlockView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="会话解锁"
        Width="460"
        Height="408"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Orderly.App;component/Views/Resources/AppContentScrollResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 进场动画 -->
            <Storyboard x:Key="LoadAnimation">
                <DoubleAnimation Storyboard.TargetName="MainCard" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="MainCard" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.5">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource LoadAnimation}" />
        </EventTrigger>
    </Window.Triggers>

    <Border Background="#FAFAFC" CornerRadius="16" BorderBrush="#E5E5EA" BorderThickness="1">
        <StackPanel x:Name="MainCard" VerticalAlignment="Center" Opacity="0" RenderTransformOrigin="0.5,0.5">
            <StackPanel.RenderTransform>
                <TranslateTransform Y="20" />
            </StackPanel.RenderTransform>

            <!-- 头像/Logo 区 -->
            <Border Width="64" Height="64" CornerRadius="32" Background="#000000" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.15" />
                </Border.Effect>
                <TextBlock Text="🔒" FontSize="26" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FFFFFF" />
            </Border>
            
            <TextBlock Text="会话已锁定" FontSize="{DynamicResource UiFontSize22}" FontWeight="Bold" Foreground="#1D1D1F" HorizontalAlignment="Center" />
            <TextBlock x:Name="TxtAccount" Margin="0,6,0,26" Foreground="#86868B" FontSize="{DynamicResource UiFontSize13}" TextWrapping="Wrap" HorizontalAlignment="Center" />
            
            <Border Width="280" Background="#F5F5F7" CornerRadius="12" BorderThickness="1" BorderBrush="Transparent" Margin="0,0,0,12">
                <Grid>
                    <PasswordBox x:Name="TxtPin" Height="52" FontSize="{DynamicResource UiFontSize24}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" KeyDown="TxtPin_KeyDown" MaxLength="6" PasswordChanged="TxtPin_PasswordChanged" />
                    <TextBlock x:Name="TxtPlaceholder"
                               Text="请输入PIN码"
                               Foreground="#C6C7CC"
                               FontWeight="SemiBold"
                               FontSize="{DynamicResource UiFontSize15}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Opacity="0.65" />
                </Grid>
            </Border>
            
            <TextBlock x:Name="TxtError" Foreground="#FF3B30" Margin="0,0,0,12" Visibility="Collapsed" TextWrapping="Wrap" HorizontalAlignment="Center" />

            <Button x:Name="BtnWindowsHello"
                    Content="使用 Windows Hello 解锁"
                    Width="280"
                    Height="42"
                    Margin="0,4,0,0"
                    Click="BtnWindowsHello_Click"
                    Visibility="Collapsed">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FFFFFF" />
                        <Setter Property="Foreground" Value="#1D1D1F" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="BorderBrush" Value="#D1D1D6" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bg"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="12">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#F5F5F7" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="bg" Property="Opacity" Value="0.75" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</Window>
