# Thread 3 Handoff Restored Baseline

Thread 6 恢复文件，用于补齐 Thread 5 指定验收路径。

当前仓库没有保留原始 Thread 3 handoff 正文；本文件不新增协议规则。Thread 3 API + SignalR 验收以 `docs/cloud-sync-sync-contract.md`、`src/Orderly.Server/Controllers/*`、`src/Orderly.Server/Hubs/WorkspaceHub.cs` 为准。

Thread 6 只修复权限、幂等、版本校验和 SignalR 边界问题，不新增协议外 API。
