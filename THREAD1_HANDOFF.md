# Thread 1 Handoff Restored Baseline

Thread 6 恢复文件，用于补齐 Thread 5 指定验收路径。

当前仓库没有保留原始 Thread 1 handoff 正文；本文件不新增协议规则。Thread 1 schema/domain 验收以 `docs/cloud-sync-v1.md`、`docs/cloud-sync-domain-model.md`、`docs/cloud-sync-schema.md` 和 DbUp migration 当前状态为准。

Thread 6 只允许修复 Blocker / High Risk，不允许重写协议。
